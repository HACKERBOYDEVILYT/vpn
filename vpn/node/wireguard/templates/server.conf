[Interface]
# NexaVPN WireGuard server
# Replace these placeholders during node provisioning.

PrivateKey = ${SERVER_PRIVATE_KEY}
Address = ${VPN_SERVER_ADDRESS}
ListenPort = ${WIREGUARD_PORT}

# Enable forwarding/NAT through the provisioning layer.
# Do not put unrestricted firewall commands directly here.

SaveConfig = false
